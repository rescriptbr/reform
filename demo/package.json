{
  "name": "demo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@reasonbr/reform": "11.0.0-alpha.1",
    "@rescript/react": "0.10.1",
    "@rescripts/cli": "0.0.14",
    "@rescripts/rescript-env": "0.0.12",
    "bs-css-emotion": "2.4.0",
    "bs-platform": "8.4.2",
    "concurrently": "5.3.0",
    "lenses-ppx": "6.1.10",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "reschema": "2.0.3"
  },
  "scripts": {
    "start": "concurrently \"yarn bs:watch\" \"yarn start:rescripts\"",
    "build": "yarn bs:build && rescripts build",
    "test": "rescripts test",
    "eject": "react-scripts eject",
    "bs:watch": "bsb -make-world -w",
    "start:rescripts": "rescripts start",
    "bs:build": "bsb -make-world",
    "bs:clean": "bsb -clean",
    "postinstall": "yarn bs:build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "rescripts": [
    "env"
  ]
}
